import { CanActivateVault.Guard } from './can-activate-vault.guard';

describe('CanActivateVault.Guard', () => {
  it('should create an instance', () => {
    expect(new CanActivateVault.Guard()).toBeTruthy();
  });
});
