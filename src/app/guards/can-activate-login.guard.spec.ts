import { CanActivateLogin.Guard } from './can-activate-login.guard';

describe('CanActivateLogin.Guard', () => {
  it('should create an instance', () => {
    expect(new CanActivateLogin.Guard()).toBeTruthy();
  });
});
